# Claude onboarding prompt

Copy and paste this prompt into Claude or another coding agent:

> Work only in `Team-Follow-Up-AI/funnel-builder-ui-public`, starting from branch `fm/public-funnel-builder-mock`. This task is fully self-contained; do not request or inspect any private repository. Read `README.md`, `AGENTS.md`, and `PROVENANCE.json` first. Run `npm install --ignore-scripts`, `npm run dev`, and `npm run validate` before editing. The project supports only the Funnels panel and uses synthetic in-memory fixtures plus localhost previews. Preserve the public sandbox banner, simulated co-author, inert forms, loopback-only server, fail-closed unknown APIs and WebSockets, and disabled Check/Commit/release behavior. Do not add credentials, real contact data, external services, provider clients, production URLs, databases, proxies, deployment workflows, or private-source references. Make changes on a new feature branch based on `fm/public-funnel-builder-mock`, test desktop and exact mobile viewport behavior, push only that branch, and open a pull request without merging. Report the exact commit, commands and results, routes checked, network boundary proof, and remaining fixture limitations.
